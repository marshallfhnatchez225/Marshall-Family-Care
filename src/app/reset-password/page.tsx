import { LockKeyhole } from "lucide-react";
import { updatePassword } from "./actions";
export default async function ResetPassword({ searchParams }: { searchParams: Promise<{ message?: string }> }) { const { message } = await searchParams; return <main className="loginpage"><section className="loginpanel"><div className="loginheading"><span className="settingicon"><LockKeyhole size={20}/></span><h1>Set up your Marshall account</h1><p>Tell us how your name should appear, then create your password.</p></div><form action={updatePassword} className="loginform">{message && <p className="formerror">{message}</p>}<label>Preferred name<input name="preferred_name" type="text" autoComplete="name" minLength={2} maxLength={100} required/></label><label>Create password<input name="password" type="password" autoComplete="new-password" minLength={10} required/></label><label>Confirm password<input name="confirm_password" type="password" autoComplete="new-password" minLength={10} required/></label><button className="primary" type="submit">Complete account setup</button></form></section></main>; }
