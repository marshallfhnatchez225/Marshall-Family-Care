import { LockKeyhole } from "lucide-react";
import { updatePassword } from "./actions";
export default async function ResetPassword({ searchParams }: { searchParams: Promise<{ message?: string }> }) { const { message } = await searchParams; return <main className="loginpage"><section className="loginpanel"><div className="loginheading"><span className="settingicon"><LockKeyhole size={20}/></span><h1>Choose a new password</h1><p>Use at least 10 characters.</p></div><form action={updatePassword} className="loginform">{message && <p className="formerror">{message}</p>}<label>New password<input name="password" type="password" autoComplete="new-password" minLength={10} required/></label><button className="primary" type="submit">Save new password</button></form></section></main>; }
