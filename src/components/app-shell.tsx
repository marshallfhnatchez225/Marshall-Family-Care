import Link from"next/link";import{Search}from"lucide-react";import{modules}from"@/lib/modules";export function AppShell({active="",children}:{active?:string;children:React.ReactNode}){return <div className="shell"><aside className="sidebar"><Link className="brand" href="/"><div className="brandmark">M</div><div><strong>Marshall OS</strong><span>Operations</span></div></Link><nav className="nav" aria-label="Main navigation">{modules.map(({slug,label,icon:Icon})=><Link key={label} href={slug?`/${slug}`:"/"} className={active===slug?"active":""}><Icon size={18}/><span>{label}</span></Link>)}</nav><div className="sidefoot"><b>Marshall Funeral Home</b>Organization workspace</div></aside><main className="main"><header className="topbar"><label className="search"><Search size={17}/><input aria-label="Search Marshall OS" placeholder="Search cases, families, services…"/></label><div className="profile"><span className="avatar">JM</span><b>Jonte Marshall<small>Administrator</small></b></div></header>{children}</main></div>}
